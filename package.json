{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "To Do Application Project",
  "default": "src/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run static && npx parcel src/index.html",
    "static": "npx gulp",
    "build": "npm run static && npx parcel build src/index.html",
    "clean:dev": "rm -rf .parcel-cache dist",
    "clean:git": "rm -rf .parcel-cache dist  node_modules"
  },
  "author": "Shirasa P",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0-rc.0",
    "gulp": "^4.0.2",
    "http-server": "^13.0.2",
    "parcel": "*",
    "sass": "^1.42.1"
  },
  "dependencies": {
    "ejs": "^2.7.4",
    "lodash": "^4.17.21",
    "uuid": "^3.4.0"
  }
}
